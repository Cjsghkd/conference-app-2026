name: KaigiApp
options:
  bundleIdPrefix: io.github.droidkaigi.confsched
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.0"
    TARGETED_DEVICE_FAMILY: "1"
packages:
  # Generated by :app-shared's swiftPMDependencies. AppShared.framework loads this package's
  # dynamic product, KotlinMultiplatformLinkedPackageDylib, which Xcode embeds in the app bundle
  # only when the target links the package.
  KotlinMultiplatformLinkedPackage:
    path: KotlinMultiplatformLinkedPackage
targets:
  KaigiApp:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: Sources
    dependencies:
      - package: KotlinMultiplatformLinkedPackage
        product: KotlinMultiplatformLinkedPackage
    info:
      path: KaigiApp-Info.plist
      properties:
        CFBundleDisplayName: DroidKaigi 2026
        # Required by Compose Multiplatform's PlistSanityCheck (it throws on launch otherwise).
        CADisableMinimumFrameDurationOnPhone: true
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
    preBuildScripts:
      - name: Embed Kotlin Framework
        script: cd "$SRCROOT/.." && ./gradlew :app-shared:embedAndSignAppleFrameworkForXcode
        basedOnDependencyAnalysis: false
      # Gradle never resolves Swift packages, so the licenses screen learns about them from the
      # iOS build; the app reads the result and hands it to createIosAppGraph.
      - name: Generate Swift Package Licenses
        script: "$SRCROOT/scripts/generate-swift-package-licenses.py"
        basedOnDependencyAnalysis: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.droidkaigi.confsched.kaigiapp
        GENERATE_INFOPLIST_FILE: NO
        FRAMEWORK_SEARCH_PATHS:
          - $(inherited)
          - $(SRCROOT)/../app-shared/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)
        OTHER_LDFLAGS:
          - $(inherited)
          - -framework
          - AppShared
        # The embedAndSign Gradle task writes into the build products; sandboxing breaks it.
        ENABLE_USER_SCRIPT_SANDBOXING: NO
