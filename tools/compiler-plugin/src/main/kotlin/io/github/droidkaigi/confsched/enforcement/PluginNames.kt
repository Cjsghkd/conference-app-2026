package io.github.droidkaigi.confsched.enforcement

// CompilerPluginRegistrar and CommandLineProcessor must declare the SAME id (unique pluginId required since Kotlin 2.3).
object PluginNames {
    const val PLUGIN_ID = "io.github.droidkaigi.confsched.enforcement"
}
